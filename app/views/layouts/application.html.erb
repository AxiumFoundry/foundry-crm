<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <title><%= content_for(:title) || "Axium Foundry - Technical Consultancy for NYC Startups" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Expert technical consultancy for ambitious NYC startups. Ship faster without hiring senior developers. Fractional CTO services, technical leadership, and engineering excellence.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/favicon.ico" sizes="32x32">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="icon" href="/icon-512.png" sizes="512x512">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gradient-dark text-white">
    <!-- Navigation -->
    <nav class="fixed w-full top-0 z-50 transition-all duration-300" data-controller="navbar">
      <div class="glass-navbar">
        <div class="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center py-4">
            <!-- Logo -->
            <div class="flex-shrink-0">
              <%= link_to root_path, class: "text-2xl font-black tracking-tight hover:opacity-80 transition-opacity" do %>
                <span class="gradient-text">Axium</span><span class="text-white"> Foundry</span>
              <% end %>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-8">
              <%= link_to "Work", case_studies_path, class: "text-gray-300 hover:text-white font-medium transition-colors" %>
              <%= link_to "Get Started", new_health_check_path,
                  class: "px-6 py-2.5 bg-gradient-primary text-gray-900 font-bold rounded-lg hover:shadow-lg hover:shadow-green-500/50 transition-all btn-glow",
                  data: { turbo_frame: "modal" } %>
            </div>

            <!-- Mobile menu button -->
            <div class="md:hidden">
              <button type="button" class="text-gray-300 hover:text-white focus:outline-none" data-action="click->navbar#toggle">
                <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
              </button>
            </div>
          </div>
        </div>

        <!-- Mobile menu -->
        <div class="hidden md:hidden" data-navbar-target="menu">
          <div class="px-2 pt-2 pb-3 space-y-1 bg-[rgba(15,10,30,0.95)] backdrop-blur-lg">
            <%= link_to "Work", case_studies_path, class: "block px-3 py-2 text-gray-300 hover:text-white font-medium transition-colors" %>
            <%= link_to "Get Started", new_health_check_path,
                class: "block px-3 py-2 bg-gradient-primary text-gray-900 font-bold rounded-lg text-center",
                data: { turbo_frame: "modal" } %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <div class="fixed top-20 right-4 z-40 space-y-2 max-w-md">
      <% if notice.present? %>
        <div class="glass border-l-4 border-green-500 p-4 animate-slide-down">
          <p class="text-green-400 font-medium"><%= notice %></p>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="glass border-l-4 border-red-500 p-4 animate-slide-down">
          <p class="text-red-400 font-medium"><%= alert %></p>
        </div>
      <% end %>
    </div>

    <!-- Main Content -->
    <main class="pt-16">
      <%= yield %>
    </main>

    <%= render "layouts/footer" %>
  </body>
</html>
