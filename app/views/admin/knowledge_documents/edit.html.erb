<section class="section-spacing-md bg-navy">
  <div class="container mx-auto max-w-4xl px-8 lg:px-16">
    <div class="border-l-4 border-primary pl-6 mb-8">
      <h1 class="text-3xl font-bold text-white mb-2">Edit: <%= @document.title %></h1>
    </div>

    <%= render "form", url: admin_knowledge_document_path(@document) %>
  </div>
</section>
